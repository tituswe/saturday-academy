import Container from '../../../common/Container';
import SyllabusCard from '../SyllabusCard';

const SyllabusList = () => {
	return (
		<Container>
			<SyllabusCard id="1" title="hi" content="world" />
		</Container>
	);
};

export default SyllabusList;
