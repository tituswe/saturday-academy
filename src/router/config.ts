const routes = [
	{
		path: ['/', '/home'],
		exact: true,
		component: 'Home',
	},
	{
		path: ['/', '/curriculum'],
		exact: true,
		component: 'Curriculum',
	},
];

export default routes;
